# Simulation

The deterministic seed creates the Rahul golden case and a coherent synthetic merchant dataset. The simulator exercises real database state through payment links, communications, provider failure records, opt-out, Promise-to-Pay, and signed webhook processing.

Golden demo credentials are `admin@recoverx.local` / `recoverx-demo` for local development only.