# AI

The recovery graph receives canonical event data and customer context, then returns structured diagnosis, root cause, action, probability, confidence, and reasoning. Deterministic risk calculations remain outside the model. Invalid or unavailable AI output must fall back to a non-consequential recommendation and must not execute a provider action.